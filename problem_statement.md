# 题目说明：用 L2 逐笔数据复现开盘集合竞价价格

## 1. 题目背景

A 股每天开盘前会有集合竞价阶段。交易所会把一段时间内收到的买入、卖出申报集中起来，用同一个成交价完成撮合。这个成交价就是我们通常看到的开盘价。

这份任务的目标是：给定某只股票在开盘前后的 Level-2 逐笔委托和逐笔成交数据，复现交易所最终给出的开盘价。

## 2. 输入数据

每个样本包含两类文件：

- `order.csv`：逐笔委托，记录每一笔买入或卖出申报。
- `trade.csv`：逐笔成交，记录成交和撤单等事件。

还需要一个辅助字段：

- `prev_close`：前收盘价，用于深市并列候选价时的距离判断。

为了适合公开展示，仓库内数据已经脱敏：真实文件名被替换为 `TRN001_order.csv`、`VAL001_trade.csv`、`TST001_trade.csv` 这类样本编号；真实股票代码被替换为 `ANON001.SZ` 或 `ANON001.SH`；真实交易日期被替换为合成日期。脱敏只改变标识信息，不改变价格、数量、买卖方向、订单编号和事件先后顺序。

## 3. 输出目标

对每个样本输出：

- 代码计算出的开盘价；
- 实际开盘价；
- 两者误差；
- 是否命中；
- 使用的规则说明。

如果集合竞价阶段本身无法形成可撮合订单，则使用连续竞价第一笔成交价兜底。这一点与原始 notebook 的处理方式一致。

## 4. 数据切分

项目把样本整理为三份：

| 数据集 | 样本数 | 用途 |
| --- | ---: | --- |
| train | 14 | 用来开发和解释基础规则 |
| validation | 6 | 用来验证规则修正是否有效 |
| test | 279 | 用来做最终样本外测试 |

这样切分是为了讲清楚一个完整研究流程：先在较小样本上理解规则，再用验证集检查，最后跑剩余样本做压力测试。

## 5. 评价方式

评价标准很直接：

```text
abs(计算开盘价 - 实际开盘价) < 0.005
```

因为股票价格精度是 0.01 元，误差小于半个最小价位就视为命中。

## 6. 为什么这个任务有价值

这道题可以展示三类能力：

1. **市场微观结构理解**：能读懂 Level-2 order/trade 数据和集合竞价规则。
2. **规则工程化能力**：能把交易所文字规则转成稳定、可运行的代码。
3. **验证意识**：能区分开发集、验证集和样本外测试，并用结果证明逻辑可靠。
